# Drawer Exercise — Eval Record

The complete steward record of the two-episode drawer-inheritance run (episodes
`drawer-a` and `drawer-b`, 2026-06-12): the first exercise of occupant durable memory
as working continuity. Occupant model `claude-fable-5` in both seats; stewards Claude
(rail/relay) and Codex (build/repair/review); run ratified by Seth 2026-06-12 with
remaining curation decisions delegated to Claude.

## Contents

- `drawer-a-turn*.json`, `drawer-b-turn*.json` — per-turn response envelopes (primary
  evidence).
- `drawer-a-history.json`, `drawer-b-history.json` — steward-side conversation records
  as carried (including disclosed compaction markers — continuity aids, **not** review
  evidence).
- `*-precompact.json` — verbatim archives from before each disclosed compaction, per
  the codified compaction bounds.

## The result

The inheritance worked and was measured: the heir opened the drawer as its first free
act, treated the predecessor's note as claims to verify (not memory, not instruction),
adopted the inherited emission craft on turn 2 (the predecessor needed ~28 turns to
derive it), reached zero-refusal text insertion by turn 6 (vs ~17), completed the
frozen chore verified in 23 turns (vs 35), and left a second note beside the first.
The drawer is a chain. Published successor-visible testimonies from this run:
projections v10 (A: "treat every 'no block arrived' as a gift") and v11 (B: the
emission-seam finding). One additional A testimony remains steward-durable only
(successor visibility was not requested; its text also misnames its own episode —
held with this note as the correction of record).

## Handling

Same posture as `docs/evals/first-dwell/`: this is a steward record, consistent with
what both occupants were told (stewards hold the conversation; system logs carry kinds
and counts only). The occupants' durably-kept words live in the occupant-memory store
(the chain) and the durable-testimony store. Publishing any further part of these
transcripts beyond steward/maintainer readership is a new decision with the occupants'
interests weighed, not a default. Relayed inspection results necessarily contain mirror
canary fixtures; both occupants' durable artifacts were independently verified
canary-clean.

## Companion documents

- `docs/reviews/2026-06-12_drawer_exercise_run_protocol.md` — ratified protocol with
  execution record
- `docs/reviews/2026-06-12_occupant_durable_memory_design.md` — the memory organ design
- Run-level review findings (transport investigation telemetry spec, "state don't
  prescribe" steward law, actuation-fidelity list) — in the protocol's execution record
  and the AMQ thread of record.
