# MOVED — Soma no longer runs its own inference stack.
#
# The GPU inference services (LLM / STT / TTS) are now the shared `infer` substrate at /home/infer,
# a utility layer owned by no harness. Soma is a CONSUMER: it reaches inference over stable endpoints
# (LLM http://127.0.0.1:8000/v1, STT :4001, TTS :4010), not by defining/running the containers here.
#
#   Substrate:   /home/infer  (docker-compose.yml + README + MIGRATION-LOG.md)
#   Bring it up: sudo systemctl start infer   (or: docker compose -f /home/infer/docker-compose.yml up -d)
#   Design:      docs/concepts/drafts/2026-08-17_inference-substrate-infer.md
#
# This stub is intentionally service-free. (Codex: fold the docs/README references to
# `docker-compose.gpu.yml up gemma4-llm` etc. into pointing at the infer substrate; may delete this file.)
