services:
  systemd-provider-test:
    build:
      context: .
      dockerfile: docker/systemd-provider-test/Containerfile
    privileged: true
    cgroup: host
    stop_signal: SIGRTMIN+3
    tmpfs:
      - /run
      - /run/lock
      - /tmp
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
